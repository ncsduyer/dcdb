<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.stylefeng.guns.modular.system.dao.WorkFlowLogMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="cn.stylefeng.guns.modular.system.model.WorkFlowLog">
        <id column="id" property="id"/>
        <result column="a_w_id" property="aWId"/>
        <result column="event_type" property="eventType"/>
        <result column="created_time" property="createdTime"/>
        <result column="step" property="step"/>
        <association property="eventStep" javaType="cn.stylefeng.guns.modular.system.model.EventStep"
                     column="{status=step,eventType=event_type}" select="selectEventStepByStatus"></association>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, a_w_id, event_type, created_time, step
    </sql>
    <select id="selectEventStepByStatus" resultMap="cn.stylefeng.guns.modular.system.dao.EventStepMapper.BaseResultMap">
        select
        <include refid="cn.stylefeng.guns.modular.system.dao.EventStepMapper.Base_Column_List"/>
        from t_tb_event_step where status=#{status} and event_type=#{eventType}
    </select>
    <select id="selectByManyId" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from t_tb_work_flow_log where a_w_id=#{id}
    </select>
</mapper>
