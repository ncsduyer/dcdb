<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.stylefeng.guns.modular.system.dao.MeetingMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="cn.stylefeng.guns.modular.system.model.Meeting">
        <id column="id" property="id"/>
        <result column="title" property="title"/>
        <result column="meeting_time" property="meetingTime"/>
        <result column="Overseer" property="Overseer"/>
        <result column="schedule" property="schedule"/>
        <result column="actual" property="actual"/>
        <result column="make" property="make"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, title, meeting_time, Overseer, schedule, actual, make
    </sql>

</mapper>
